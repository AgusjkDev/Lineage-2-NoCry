type Anchor = {
    children: string;
    href: string;
};

type Feature = {
    legend: string;
    icon: string;
};

type ImportantDate = {
    background: string;
    legend: string;
    date: string;
};
